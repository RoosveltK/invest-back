# Core
Django>=5.0,<5.2
djangorestframework>=3.15
psycopg2-binary>=2.9

# Auth — [with_social] pulls in requests, oauthlib, python-jose, etc.
djangorestframework-simplejwt>=5.3
dj-rest-auth[with_social]>=6.0
django-allauth[socialaccount]>=0.63
requests>=2.31

# Async / Real-time
celery>=5.4
redis>=5.0
channels>=4.1
channels-redis>=4.2
daphne>=4.1

# Celery Beat scheduler (django_celery_beat in INSTALLED_APPS)
django-celery-beat>=2.6

# API Docs
drf-spectacular>=0.27

# Utilities
django-cors-headers>=4.3
Pillow>=10.0
python-decouple>=3.8

# Testing
pytest>=8.0
pytest-django>=4.7
factory-boy>=3.3
Faker>=24.0
pytest-cov>=5.0
